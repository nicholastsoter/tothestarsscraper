[project]
name = "tothestarsscraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "python-dotenv>=1.0.0",
    "googlemaps>=4.10.0",
    "requests>=2.31.0",
    "psycopg[binary]>=3.1",
]

[tool.vercel]
entrypoint = "webapp.main:app"
